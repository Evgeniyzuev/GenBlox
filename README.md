# GenBlox

Браузерная платформа маленьких игровых пространств. Первый прототип включает
пиксельное меню и спейс «Крестики-нолики» на двух игроков.

## Локальный запуск

Проект не требует сборки или установки зависимостей:

```powershell
python -m http.server 3000
```

Затем откройте `http://localhost:3000`.

Для публикации на Vercel достаточно импортировать репозиторий как статический сайт.

## Файлы приложения

- `index.html` — меню и разметка игрового спейса.
- `styles.css` — пиксельный интерфейс и адаптивная вёрстка.
- `app.js` — игровая логика крестиков-ноликов.

## Сетевая игра

1. На первом устройстве нажать «Играть» → «Создать комнату».
2. Передать появившийся шестизначный код второму игроку.
3. На втором устройстве нажать «Играть», ввести код и выбрать «Войти».

Соединение между браузерами работает через Cloudflare Worker и Durable Object.
Отдельная база данных не требуется: комнаты автоматически удаляются после
отключения их владельцев.

Доступны два типа комнат:

- открытая — видна всем игрокам в списке;
- приватная — доступна только по шестизначному коду.

Перед входом второго игрока владелец комнаты принимает или отклоняет запрос.

### Локальный запуск realtime-сервера

```powershell
cd worker
npm install
npm run dev
```

Сайт автоматически использует `ws://localhost:8787/ws` при локальном запуске.

### Развёртывание на Cloudflare

1. Отправить репозиторий в GitHub или GitLab.
2. В Cloudflare открыть **Workers & Pages → Create application**.
3. Импортировать репозиторий и выбрать корневой каталог `worker`.
4. Указать deploy command: `npx wrangler deploy`.
5. После первого деплоя скопировать адрес `workers.dev`.
6. Заменить `YOUR-SUBDOMAIN` в `config.js` на поддомен своего аккаунта.
7. Повторно опубликовать статический сайт на Vercel.

Привязка Durable Object и её первая миграция уже описаны в
`worker/wrangler.jsonc`.

## Процесс разработки

Документы процесса разработки:

- [План-концепт](PLAN.md) — цели, границы, этапы и критерии готовности проекта.
- [Канбан](KANBAN.md) — текущее состояние задач и правила движения карточек.

## Как работать с документами

1. Перед началом этапа уточнить соответствующий раздел в `PLAN.md`.
2. Добавить конкретные задачи этапа в `KANBAN.md`.
3. Во время работы перемещать задачи между колонками канбана.
4. После завершения этапа обновить прогресс и зафиксировать принятые решения.
